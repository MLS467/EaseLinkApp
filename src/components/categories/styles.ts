import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    height: 52,
    maxHeight: 52,
    flexDirection: "row",
  },
  content: {
    gap: 16,
    paddingHorizontal: 24,
    color: "#fff",
  },
});
