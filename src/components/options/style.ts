import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  containerOption: {
    flexDirection: "row",
    alignItems: "center",
    gap: 2,
  },

  textOption: {
    fontSize: 20,
  },
});
