import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: "auto",
    alignItems: "center",
    flexDirection: "row",
    padding: 10,
    gap: 5,
  },
  icon: {},
  text: {
    fontSize: 16,
  },
});
